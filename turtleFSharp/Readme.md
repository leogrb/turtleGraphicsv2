# How to build

- restore content dependencies via ```.paket\paket.exe restore```.
- when using visual studio, open the solution in TurtleGraphics folder.
- when using vscode use ``build build`` to build, and ``build run`` to run.